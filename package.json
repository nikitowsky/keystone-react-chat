{
  "name": "keychat",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "dotenv -e .env.development -- turbo run dev --parallel",
    "test": "dotenv -e .env.test -- turbo run test",
    "turbo": "turbo"
  },
  "devDependencies": {
    "turbo": "1.5.6"
  },
  "dependencies": {
    "dotenv-cli": "^6.0.0"
  }
}
